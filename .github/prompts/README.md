# Agent Prompts

Blocks uses `agents/skills/` as canonical source and generates runtime skill catalogs with `agents/tools/skills/sync-agent-skills.ps1`.

GitHub Copilot-compatible skills are discovered through `.agents/skills/`; do not maintain a second prompt payload here.
